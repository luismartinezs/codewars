// Loop over the array, for each value, compare it to the previous one, if it is smaller, swap it. Then compare it again with the previous value. Do this until the previous value is smaller. Then go to the next value
// Loop over array items
// Store current value and index
// Loop over previous values while the value is larger than the current value, and shift them forward by one position in the array
// Assing the position that was "left empty" to the current value
function insertionSort2(arr) {}

export default insertionSort2
