// Check each pair of numbers of the array in order, if the one in the left is larger than the one in the right, swap them
// If no swaps were done that means the array is sorted already
// in the first iteration, the largest number will end up at the last position for sure, so we know it will be already sorted
function bubble(arr) {}

export default bubble
