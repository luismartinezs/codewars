// ordered array, unrepeated numbers
// check middle number, based on which we halve the part of the array we look into
// Tips:
// - Use a getMid helper function
// - Consider for even subarray lengths whether we pick the floor or ceil value, that's a choice
function binarySearch(sortedArr, value) {}

export default binarySearch
